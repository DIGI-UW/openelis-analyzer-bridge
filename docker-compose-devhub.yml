version: "3.8"

x-logging: &local-logging
  driver: "local"
  options:
    max-size: "20m"
    max-file: "50"

services:
  openelis-analyzer-bridge:
    container_name: openelis-analyzer-bridge
    image: itechuw/openelis-analyzer-bridge:develop
    pull_policy: always
    ports:
      - "8442:8443"
      - "12000:12001"
    volumes:
      - ./configuration.yml:/app/configuration.yml
    logging: *local-logging
    environment:
      SPRING_PROFILE: dev
      # INFO, not TRACE: the vendored ASTM library logs whole clinical messages at TRACE, so that
      # level puts patient results into ordinary container logs.
      LOGGING_LEVEL_ORG_ITECH: INFO
    healthcheck:
      test: ["CMD", "/app/healthcheck.sh"]
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 2m
