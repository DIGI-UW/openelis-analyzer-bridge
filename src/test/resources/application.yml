# Test configuration for Spring Boot tests
org:
  itech:
    ahb:
      forward-http-server:
        uri: http://localhost:8080/test
      forward-astm-server:
        host-name: localhost
        port: 5000

server:
  port: 0  # Random port for tests

logging:
  level:
    root: WARN
    org.itech: INFO
