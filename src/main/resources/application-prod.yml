server:
  port: 8443
  ssl:
    enabled: true
    keyStoreType: ${server.ssl.keyStoreType}
    key-store: ${server.ssl.keyStorePath}
    key-store-password: ${server.ssl.keyStorePassword}
logging:
  level:
    root: INFO

# MLLP Server Configuration (HL7 v2.x transport via HAPI SimpleServer)
# Disabled by default in production for safety. Set MLLP_ENABLED=true
# in the environment to enable. This aligns with MLLPConfig default (enabled=false).
org.itech.ahb.mllp:
  enabled: ${MLLP_ENABLED:false}
  port: ${MLLP_PORT:2575}
  timeout: ${MLLP_TIMEOUT:30000}
  max-message-size: ${MLLP_MAX_MESSAGE_SIZE:1048576}
